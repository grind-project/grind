# grind
grind est une plateforme de diffusion de connaissances

## Configuration API Tests

1. Copiez `api.http.env.example.json` en `api.http.env.json`
2. Modifiez les valeurs dans `api.http.env.json` avec vos credentials